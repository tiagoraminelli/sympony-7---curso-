<form method="post" action="{{ path('app_presupuestos_detalle_delete', {'id': presupuestos_detalle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presupuestos_detalle.id) }}">
    <button class="btn">Delete</button>
</form>
